create user if not exists 'ki-oscar'@'127.0.0.1' identified by '~ki-oscar-password~'
grant select
 , insert
 , update
 , delete
 , create
 , drop
 , create temporary tables
  on oscar.*
  to 'ki-oscar'@'127.0.0.1'
;
create user if not exists 'ki-oscark'@'127.0.0.1' identified by '~ki-oscark-password~'
grant select
  on kalipso.*
  to 'ki-oscark'@'127.0.0.1'
